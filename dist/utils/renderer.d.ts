import { default as React } from 'react';

export declare function renderToHTML(element: React.ReactElement): string;
export declare function renderToDOM(element: React.ReactElement, containerId: string): void;
